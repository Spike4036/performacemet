{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run EMOTIV Bridge",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/bridge.py",
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}",
      "envFile": "${workspaceFolder}/.env",
      "justMyCode": true
    }
  ]
}
